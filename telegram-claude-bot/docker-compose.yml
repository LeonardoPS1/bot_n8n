version: '3.8'

services:
  telegram-claude-bot:
    build: .
    container_name: claude-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot.log:/app/bot.log
    ports:
      - "8080:8080"  # Health check endpoint (if added)
